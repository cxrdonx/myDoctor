<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>
</head>
<body class="container">
    <header>
        <%- include('../partials/header'); %>
        <style>
  .card{
      margin-bottom: 50px;
  }
  .row{
      height: 700px;
      border-color: 3px solid rgb(37, 37, 37);
  }

.logo {
  width: 30%;
}

        </style>
    </header>
<div class="row">
    <% for(var i=0; i<notes.length; i++) {%>
    <div class="col-md-3">
      <div class="card">
        <div class="card-body">
          <h4 class="card-title d-flex justify-content-between align-items-center">
            <%=notes[i].title %> <a href="/notes/edit/{{_id}}"><i class="fas fa-edit"></i></a>
          </h4>
          <p><%=notes[i].description%> </p>
          <!-- DELETE REQUEST -->
          <form action="/notes/delete/<%=notes[i]._id%>?_method=DELETE" method="POST">
            <input type="hidden" name="_method" value="DELETE">
            <button type="submit" class="btn btn-danger btn-block btn-sm">Eliminar</button>
          </form>
        </div>
      </div>
    </div>
    <% } %>
    <div class="card mx-auto">
      <div class="card-body">
        <h1>Hola</h1>
        <p clsss="lead">:¿quieres agregar una nota nueva?</p>
        <a href="/notes/add" class="btn btn-success btn-block">Create One!</a>
      </div>
    </div>
   
</div>
</body>
<footer>
    <%- include('../partials/footer'); %>
</footer>
</html>